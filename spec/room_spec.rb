require File.expand_path(File.dirname(__FILE__) + '/spec_helper')

describe "Room" do
  context "when given a direction" do
    it "should return the room in that direction"
  end
  
  context "when given a direction for a non-existent exit" do
    it "should return an error"
  end
end
